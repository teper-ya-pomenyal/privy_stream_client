# Handoff: privy_stream — десктопный клиент музыкального стриминга

## Overview

privy_stream — федеративный музыкальный стриминг. Единого сервера нет: любой поднимает свой узел (node), заливает туда фонотеку и раздаёт адрес; клиент подключается к узлу и слушает/ищет музыку только на нём. Позиционирование — прослушивание без региональных блокировок и внешних списков запрещённого, приватность по умолчанию (история не пишется, телеметрии нет).

Задача: реализовать **десктопное приложение** (первая цель) по приложенному макету. Рекомендуемый стек — **Tauri + Vite + React + TypeScript** (нативный webview, бинарь ~10 МБ, Rust-сторона нужна для keychain-хранения токенов по узлам, дискового кэша аудио и кастомного протокола для стрима в обход CORS). Electron — только если понадобятся node-модули в рантайме.

Бэкенд пишет сам заказчик (Go: user_service — auth c JWT RS256 — готов, gateway в работе). Клиент должен быть «пустым»: список узлов и ключи живут локально, baseURL подставляется из выбранного узла.

## About the Design Files

Файл `Privy Stream.dc.html` в этом бандле — **дизайн-референс, сделанный в HTML**. Это прототип, показывающий внешний вид и поведение, а не продакшн-код для копирования. Задача — **воссоздать этот дизайн в целевом окружении** по его правилам (компоненты, роутинг, стили, менеджер состояния), а не встраивать HTML как есть.

Технически файл — компонент со шаблоном и классом логики: разметка эквивалентна JSX (атрибуты уже camelCase, `style="…"` компилируется в объект стилей), `<sc-for list as>` = `.map()`, `<sc-if value>` = условный рендер, `{{ x }}` = значение из `renderVals()`. Класс логики — это React class component без `render()`: `state`, `setState`, `componentDidMount`. Перенос в `.tsx` механический; `renderVals()` переносится почти дословно (или раскладывается на хуки/селекторы).

Открыть макет: `Privy Stream.dc.html` и `support.js` должны лежать рядом, открывать через локальный сервер (`npx serve .`), не по `file://`.

## Fidelity

**High-fidelity.** Цвета, типографика, отступы, размеры и состояния — финальные, воссоздавать точно. Все значения перечислены ниже в Design Tokens. Обложки и фото артистов — плейсхолдеры (диагональная штриховка с подписью и указанием размера); в приложении на их месте реальные изображения с узла, плейсхолдер остаётся как состояние загрузки/отсутствия арта.

## Screens / Views

Макет содержит 6 экранов: Auth, Servers, Catalog, Album, Artist, Library, плюс два состояния плеера (нижняя панель и полноэкранный) и мобильный вид (не переносить в десктоп, см. «Что не переносить»).

---

### 1. Auth — вход / регистрация

**Purpose:** выбрать узел подключения, войти или создать аккаунт на этом узле.

**Layout:** две колонки на весь viewport, `grid-template-columns: 1.05fr .95fr`, высота `100vh`, `overflow: hidden`.

**Левая колонка** — фон `#0E0E10`, `border-right: 1px solid #1E1E22`, `padding: 56px 60px`, `flex-direction: column`, `justify-content: space-between` (три блока: лого сверху, смысловой блок по центру, техстрока снизу):

- Лого: квадрат 12×12 `#D8452B` + текст `PRIVY/STREAM`, Manrope 700 13px, `letter-spacing: .22em`, слэш цветом `#6E6C67`. Gap 10px.
- Центральный блок, `max-width: 460px`, gap 26px:
  - Надзаголовок: `БЕЗ ГРАНИЦ · БЕЗ ЦЕНЗУРЫ`, IBM Plex Mono 600 11px, `letter-spacing: .2em`, `#D8452B`.
  - H1: «Ты сам решаешь / какую музыку / слушать» (переносы принудительные), Manrope 700 52px, `line-height: .98`, `letter-spacing: -.03em`.
  - Абзац: «Приватная фонотека без региональных блокировок и внешних списков запрещённого. Твой каталог, твой ключ, твоё правило.» Manrope 400 15px/1.6, `#8E8C86`, `max-width: 390px`.
  - Три поля метаданных в ряд (gap 28px) над `border-top: 1px solid #1E1E22`: ШИФРОВАНИЕ / RS256, СЕССИЯ / РОТАЦИЯ, ТЕЛЕМЕТРИЯ / ВЫКЛ. Подпись — Mono 600 10px `letter-spacing: .14em` `#6E6C67`, значение — Mono 500 13px `#E8E6E1`.
- Нижняя строка: `{host} · UPTIME 99.97% · BUILD 0.4.1`, Mono 400 10px/1.6, `#4E4C48`, `letter-spacing: .1em`. Host — адрес выбранного узла.

**Правая колонка** — карточка по центру, `max-width: 392px`, фон `#121214`, `border: 1px solid #26262A`, без радиуса:

- Табы ВХОД / РЕГИСТРАЦИЯ: два равных блока, `padding: 16px 0`, Mono 600 11px `letter-spacing: .16em`, активный — `#E8E6E1` на `#17171A`, неактивный — `#6E6C67`; разделитель `border-left: 1px solid #26262A`, снизу `border-bottom: 1px solid #26262A`.
- Тело: `padding: 28px 26px 26px`, gap 18px.
- **Блок выбора узла** (первый, до полей): лейбл `УЗЕЛ ПОДКЛЮЧЕНИЯ` (Mono 600 10px `.16em` `#6E6C67`), сетка чипов `1fr 1fr`, gap 7px. Чип: `padding: 9px 11px`, `border: 1px solid #2E2E33`, Mono 500 11px `#8E8C86`, gap 7px, внутри — статус-точка 7×7 и имя узла с ellipsis. Активный чип: `border-color: #D8452B`, текст `#F2F0EB`, фон `rgba(216,69,43,.08)`. Под сеткой — подпись `{host} · аккаунт создаётся на выбранном узле`, Mono 400 10px `#4E4C48`. Ниже разделитель `height: 1px; background: #1E1E22`.
- Поля ЛОГИН (`user_name`), ПАРОЛЬ (`type=password`, `••••••••••`), и в режиме регистрации — ДАТА РОЖДЕНИЯ (`1994-05-17`). Лейбл — Mono 600 10px `.16em` `#6E6C67`, gap до поля 8px. Инпут: `padding: 12px 13px`, фон `#0C0C0E`, `border: 1px solid #2E2E33`, текст `#E8E6E1` Mono 500 14px, `outline: none`.
- Ошибка (если есть): `padding: 10px 12px`, фон `rgba(216,69,43,.1)`, `border-left: 2px solid #D8452B`, Mono 500 11px/1.4, `#F0765C`.
- Кнопка: `padding: 14px 0`, фон `#D8452B`, текст `#0B0B0C` Mono 700 12px `letter-spacing: .18em`, во всю ширину. Подпись — `ВОЙТИ` либо `СОЗДАТЬ КЛЮЧ` в режиме регистрации. Hover: фон `#F0563A`.
- Футер карточки над `border-top: 1px solid #1E1E22`: слева `POST /v1/auth`, справа `REFRESH · РОТАЦИЯ` (в регистрации — `RS256 · КЛЮЧ ЛОКАЛЬНО`), Mono 400 10px `#4E4C48`.

**Валидация (как в макете):** узел не online → `503 · узел {host} не отвечает`; пустой логин или пароль короче 4 символов → `401 · логин или пароль неверны`; в регистрации дата не по `^\d{4}-\d{2}-\d{2}$` → `400 · birth_date: ожидается YYYY-MM-DD`. В приложении заменить на реальные коды ответов узла, сохранив формат «код · сообщение» и место вывода.

---

### 2. Servers — сеть узлов

**Purpose:** ключевой экран продукта. Выбрать узел, к которому подключён клиент; добавить чужой или свой узел по адресу.

**Layout:** контент-колонка приложения, `padding: 34px 44px 46px` (в компактном режиме `20px 26px 30px`).

- **Шапка:** `display: flex; flex-wrap: wrap; justify-content: space-between; align-items: flex-end; gap: 30px`, `padding-bottom: 20px`, `border-bottom: 1px solid #1E1E22`.
  - Слева: надзаголовок `СЕТЬ УЗЛОВ · {n} ОНЛАЙН` (Mono 600 10px `.2em` `#D8452B`) + H2 «Серверы» (Manrope 700 34px, `letter-spacing: -.02em`).
  - Справа: абзац `max-width: 420px`, Manrope 400 13px/1.6 `#8E8C86`: «Единого сервера нет. Любой может поднять свой узел, залить туда фонотеку и раздать адрес. Клиент подключается к узлу и ищет музыку только на нём.»
- **Добавление узла** (`padding: 22px 0 26px`, gap 14px): лейбл `ПОДКЛЮЧИТЬСЯ ПО АДРЕСУ` (Mono 600 10px `.18em` `#4E4C48`); ряд с переносом, gap 10px:
  - Поле: `flex: 1 1 300px; max-width: 420px`, `border: 1px solid #26262A`, фон `#101012`, `padding: 11px 13px`, префикс `://` (Mono 600 11px `#4E4C48`), инпут Mono 500 13px, placeholder `10.0.0.5:8443`.
  - Кнопка `ДОБАВИТЬ УЗЕЛ`: `padding: 13px 20px`, фон `#D8452B`, `#0B0B0C`, Mono 700 11px `.14em`, `white-space: nowrap`.
  - Подпись справа: «ключ и история остаются / на стороне клиента», Mono 400 10px/1.5 `#4E4C48`.
  - Ошибка — тот же стиль, что в Auth.
- **Список узлов** над `border-top: 1px solid #1E1E22`. Каждая строка — **переносящаяся карточка** (не табличная сетка; колонки специально убраны, чтобы адрес и описание не обрезались): `display: flex; flex-wrap: wrap; gap: 14px 18px; align-items: center`, `padding: 17px 14px` (компактно — `12px 14px`), `border-bottom: 1px solid #17171B`, `border-left: 2px solid transparent`.
  - Активная (подключённая) строка: фон `rgba(216,69,43,.07)`, `border-left-color: #D8452B`.
  - Левая часть: `flex: 1 1 320px; min-width: 0`, gap 12px, `align-items: flex-start`. Статус-точка 7×7 (`margin-top: 7px`) + колонка gap 6px:
    - Первая строка (`display: flex; align-items: baseline; gap: 10px; flex-wrap: wrap`): имя узла Manrope 600 15px, адрес Mono 500 11px `#C8C6C0` c `overflow-wrap: anywhere`.
    - Вторая строка: `{владелец} · {тип доступа} · {описание}`, Mono 400 10px/1.5 `#5A5854`, `text-wrap: pretty`.
  - Правая часть: `margin-left: auto`, gap 18px, `align-items: center`. Колонка статуса (`text-align: right`, gap 5px): статус Mono 500 9px `.12em` цветом статуса, `white-space: nowrap`; пинг Mono 500 11px `#6E6C67`. Затем кнопка действия: `padding: 9px 13px`, `border: 1px solid #2E2E33`, Mono 600 10px `.12em` `#C8C6C0`; у текущего узла — `border-color`/текст `#D8452B` и `cursor: default`.
- **Футер экрана** (`padding-top: 22px`, `display: flex; flex-wrap: wrap; gap: 26px`, Mono 400 10px/1.6 `#4E4C48`, `letter-spacing: .08em`): три тезиса — `РЕЛИЗЫ ИНДЕКСИРУЮТСЯ УЗЛОМ, НЕ КЛИЕНТОМ`, `ПОИСК ИДЁТ ТОЛЬКО ПО ТЕКУЩЕМУ УЗЛУ`, `СМЕНА УЗЛА НЕ ТРОГАЕТ ЛОКАЛЬНУЮ ФОНОТЕКУ`.

**Статусы и их цвета:**

| Состояние | Подпись | Цвет | Точка |
|---|---|---|---|
| Подключён (текущий) | `ПОДКЛЮЧЁН` | `#D8452B` | `#D8452B` |
| Идёт подключение | `ПОДКЛЮЧЕНИЕ…` | `#E8C24B` | мигает (анимация `pv-blink` 0.7s infinite) |
| Online, не выбран | `ДОСТУПЕН` | `#7FAE6B` | `#7FAE6B` |
| Не отвечает | `НЕ ОТВЕЧАЕТ` | `#5A5854` | `#3A3A40` |

Кнопка: `ТЕКУЩИЙ` / `ПОДКЛЮЧИТЬСЯ` / `ПОВТОР` (для offline).

**Данные узлов в макете** (пример структуры; в приложении приходят из `/.well-known/`-дескриптора узла или локального списка):

| id | name | host | owner | доступ | ping | status | описание |
|---|---|---|---|---|---|---|---|
| eu3 | open.eu3 | eu3.privy.stream:8443 | alt_arkhiv | открытый | 12 | online | публичный узел, регистрация свободная |
| ru-ind | independent | ind.local:9000 | k_volkov | по инвайту | 34 | online | самиздат-лейблы, ключ по приглашению |
| nl-vault | vault-nl | 89.211.4.77:8443 | vault_op | открытый | 78 | online | архив редких изданий, отдаёт FLAC 24/96 |
| home | мой домашний | 192.168.1.14:8443 | ты | приватный | 2 | offline | узел на твоём железе, виден только тебе |

**Поведение:**
- Клик по строке/кнопке online-узла → статус `ПОДКЛЮЧЕНИЕ…` с мигающей точкой, через ~650 мс подключение: активным становится новый узел, приложение уходит на Catalog, поиск и фильтр сбрасываются. В приложении заменить таймаут реальным запросом к узлу (handshake + получение дескриптора), сохранив промежуточное состояние.
- Клик по offline-узлу → ошибка `503 · {host} не отвечает`, подключение не происходит.
- Добавление узла: адрес валидируется по `^[\w.\-]+(:\d+)?$`, иначе `400 · адрес вида host:port, например 10.0.0.5:8443`; дубликат по host → `узел уже в списке`. Новый узел появляется в списке со статусом online, ping 20–80, тип доступа `неизвестно`, описание «узел добавлен вручную, метаданные ещё не получены», владелец «добавлен тобой». В приложении: пробить узел запросом, взять имя/владельца/тип доступа из его дескриптора, реальный ping измерить.

---

### 3. Catalog — полка текущего узла

**Purpose:** просмотр и поиск релизов **только на подключённом узле**.

**Layout:** `padding` как у Servers.

- Шапка (`flex`, `flex-wrap: wrap`, `align-items: flex-end`, `justify-content: space-between`, gap 30px, `border-bottom: 1px solid #1E1E22`, `padding-bottom: 20px`):
  - Слева: `{host} · {n} РЕЛИЗОВ` (Mono 600 10px `.2em` `#D8452B`) + H2 `Полка узла «{name}»` (Manrope 700 34px `-.02em`).
  - Справа: поиск — `flex: 1 1 260px; max-width: 340px`, `border: 1px solid #26262A`, фон `#101012`, `padding: 10px 13px`, gap 10px: префикс `/` (Mono 600 11px `#4E4C48`), инпут Mono 500 13px с placeholder `артист, релиз, метка…`, справа счётчик `{найдено}/{всего}` Mono 400 10px `#4E4C48`.
- Фильтры (`padding-top: 18px`, gap 8px, с переносом): `ВСЁ`, `НОВОЕ`, `ЗАПРЕЩЁННОЕ ГДЕ-ТО`, `FLAC`, `ЛОКАЛЬНОЕ`. Чип: `padding: 7px 12px`, `border: 1px solid #26262A`, Mono 600 10px `.12em` `#8E8C86`; активный — рамка и текст `#D8452B`. (`НОВОЕ` = год ≥ 2024, `ЗАПРЕЩЁННОЕ ГДЕ-ТО` = релизы с меткой 18+/без фильтра; `FLAC` и `ЛОКАЛЬНОЕ` в макете декоративны — в приложении фильтровать по формату и наличию в локальной фонотеке.)
- Сетка карточек: `grid-template-columns: repeat(auto-fill, minmax(194px, 1fr))`, gap 26px, `padding-top: 30px` (компактно: `minmax(150px, 1fr)`, gap 16px, `padding-top: 20px`).
- Карточка релиза (gap 10px, `cursor: pointer`, hover `opacity: .82`, появление — анимация `pv-rise` .3s ease):
  - Обложка: `aspect-ratio: 1`, плейсхолдер `repeating-linear-gradient(135deg, #17171A 0 7px, #121215 7px 14px)`, `border: 1px solid #26262A`. Внутри подпись «обложка / 1000×1000» Mono 500 9px/1.5 `#5A5854` по центру. В левом верхнем углу — каталожный код на подложке `#0B0B0C`, `padding: 4px 6px`, Mono 600 9px `#8E8C86`. Для помеченного контента в правом нижнем углу — `18+` на `#D8452B`, текст `#0B0B0C`, Mono 700 8px `.1em`.
  - Подписи (gap 3px): название Manrope 600 14px `-.01em`; артист Mono 400 11px `#8E8C86`; мета `{год} · {ЖАНР}` Mono 400 10px `#4E4C48` `.08em`.
- **Пустое состояние** (узел ничего не раздаёт или поиск ничего не нашёл): блок `margin-top: 30px`, `border: 1px dashed #2E2E33`, `padding: 34px`, gap 14px, `align-items: flex-start`: надпись `ПУСТОЙ ИНДЕКС` (Mono 600 10px `.18em` `#6E6C67`), текст Manrope 500 16px/1.5 `#C8C6C0` `max-width: 420px` — «Узел «{name}» пока ничего не раздаёт — владелец не залил фонотеку.» либо «На этом узле ничего не найдено по запросу.», и кнопка-обводка `ВЫБРАТЬ ДРУГОЙ УЗЕЛ` → на Servers.

**Релизы в макете** (8 шт., распределены по узлам): AR-014 «Полынный свет» / Тихая Комната / 2024 / эксперимент / eu3; AR-021 «Запрещённые письма» / Северный Шум / 2023 / пост-панк / 18+ / ru-ind; AR-033 «Ночной эфир» / Мальва / 2025 / эмбиент / eu3; AR-047 «Красная линия» / Отдельный Канал / 2022 / нойз / 18+ / nl-vault; AR-052 «Молчание не входит» / Кама / 2024 / фолк / eu3; AR-060 «Свободная частота» / Тихая Комната / 2021 / краут / ru-ind; AR-066 «Без подписи» / Гость / 2025 / драм-н-бэйс / 18+ / nl-vault; AR-071 «Территория» / Мальва / 2023 / минимал / eu3.

---

### 4. Album — страница релиза

**Purpose:** послушать релиз, посмотреть треклист и технические метаданные.

**Layout:** `padding` как у Servers. Сверху ссылка возврата `← КАТАЛОГ` (Mono 600 10px `.14em` `#8E8C86`, hover `#E8E6E1`, `margin-bottom: 26px`). Далее `display: flex; flex-wrap: wrap; gap: 34px; align-items: flex-start`.

**Левая колонка** — `flex: 1 1 220px; max-width: 264px`, gap 14px:
- Обложка `aspect-ratio: 1`, тот же плейсхолдер-паттерн, `border: 1px solid #26262A`, подпись «обложка релиза / 1000×1000».
- Таблица метаданных в рамке `1px solid #26262A`; строка: `justify-content: space-between`, `padding: 9px 12px`, `border-bottom: 1px solid #1B1B1F`; ключ Mono 400 10px `.1em` `#6E6C67`, значение Mono 500 10px `#C8C6C0`. Поля: КАТАЛОГ, ГОД, ЖАНР, ФОРМАТ (`FLAC 24/96`), **УЗЕЛ** (имя узла, с которого отдаётся релиз), ГЕОБЛОК (`НЕТ`), МЕТКА (`18+ / БЕЗ ФИЛЬТРА` либо `БЕЗ МЕТОК`).

**Правая колонка** — `flex: 1 1 420px; min-width: 0`, gap 24px:
- Артист как ссылка: `{Артист} →`, Mono 600 11px `.16em` `#D8452B`, hover `#E8E6E1`, клик → Artist.
- H2 название: Manrope 700 46px, `line-height: .98`, `letter-spacing: -.03em`.
- Мета-строка: `{год} · {ЖАНР} · {n} ТРЕКА · 38:41`, Mono 400 12px `#8E8C86` `.08em`.
- Кнопки (gap 10px, с переносом, все `white-space: nowrap`): `▶ СЛУШАТЬ` — `padding: 13px 22px`, фон `#D8452B`, `#0B0B0C`, Mono 700 11px `.16em`, hover `#F0563A` (запускает первый трек релиза); `+ В БИБЛИОТЕКУ` и `СКАЧАТЬ FLAC` — `padding: 13px 18px`, `border: 1px solid #2E2E33`, Mono 600 11px `.14em` `#C8C6C0`.
- Треклист над `border-top: 1px solid #1E1E22`. Заголовок: `grid-template-columns: 30px minmax(0,1fr) auto 52px`, gap 14px, `padding: 11px 10px`, Mono 600 9px `.16em` `#4E4C48`, `border-bottom: 1px solid #1E1E22`: `#`, `НАЗВАНИЕ`, `ФОРМАТ`, `ВРЕМЯ` (справа).
- Строка трека — та же сетка, `padding: 12px 10px` (компактно 8px), `border-bottom: 1px solid #17171B`, `border-left: 2px solid transparent`, `cursor: pointer`, hover `background: rgba(255,255,255,.03)`. Номер Mono 500 11px `#5A5854`; название Manrope 600 13px `#C8C6C0` с ellipsis и подписью `{артист} · {формат}` Mono 400 10px `#5A5854`; формат Mono 400 10px `#6E6C67`; время Mono 500 11px `#8E8C86` справа.
- **Играющий трек:** фон строки `rgba(216,69,43,.08)`, `border-left-color: #D8452B`, номер `#D8452B`, название `#F2F0EB`.

---

### 5. Artist — страница артиста

**Purpose:** дискография и популярные треки артиста.

**Layout:**
- **Герой:** высота 280px, фон-плейсхолдер `repeating-linear-gradient(115deg, #16161A 0 9px, #111114 9px 18px)`, `border-bottom: 1px solid #1E1E22`, контент прижат книзу (`padding: 0 44px 26px`). В левом верхнем углу подпись «фото артиста · 2400×720» (Mono 500 10px `#5A5854`), в правом верхнем — `← НАЗАД`. Контент (gap 14px): плашка `● АРТИСТ · БЕЗ ГЕОБЛОКА` (точка 6×6 `#D8452B` + Mono 600 10px `.2em` `#D8452B`), имя Manrope **800 68px**, `line-height: .92`, `letter-spacing: -.04em`, мета `{n} РЕЛИЗА · 2019—2025 · НЕЗАВИСИМО` Mono 400 12px `#8E8C86`.
- **Статистика:** `grid-template-columns: repeat(auto-fit, minmax(160px, 1fr))`, `gap: 1px`, фон и рамка `#1E1E22` (гэп работает как разделитель), `margin-bottom: 34px`. Ячейка: фон `#0E0E10`, `padding: 16px 18px`, gap 7px: ключ Mono 600 9px `.16em` `#6E6C67`, значение Manrope 600 22px. Поля: СЛУШАТЕЛЕЙ `48 210`, РЕЛИЗОВ `{n}`, СТРАН БЕЗ БЛОКА `182`, В БИБЛИОТЕКАХ `9 340`.
- **Две колонки:** `grid-template-columns: repeat(auto-fit, minmax(320px, 1fr))`, gap 40px.
  - `ПОПУЛЯРНОЕ` — те же строки треков, что на Album (сетка `30px minmax(0,1fr) auto 52px`, без подписи под названием).
  - `ДИСКОГРАФИЯ` — сетка `1fr 1fr`, gap 16px; мини-карточка: обложка-плейсхолдер `aspect-ratio: 1`, название Manrope 600 12px, мета Mono 400 10px `#6E6C67`; клик → Album.
- Заголовки секций: Mono 600 10px `.18em` `#4E4C48`.

---

### 6. Library — локальная фонотека

**Purpose:** то, что скачано локально; **не зависит от выбранного узла** (смена узла её не меняет).

**Layout:** шапка как у Catalog: надзаголовок `ЛОКАЛЬНОЕ ХРАНИЛИЩЕ · НЕ ИНДЕКСИРУЕТСЯ` (`#D8452B`), H2 «Библиотека», справа — Mono 400 11px/1.6 `#6E6C67` `text-align: right`: «{n} треков · 14.2 ГБ / синхронизация 3 устройства». `border-bottom: 1px solid #1E1E22`, `margin-bottom: 22px`.

Таблица: `grid-template-columns: 30px minmax(0,1.4fr) minmax(0,1fr) minmax(0,1fr) auto 52px`, gap 14px. Заголовки: `#`, `НАЗВАНИЕ`, `АРТИСТ`, `РЕЛИЗ`, `ФОРМАТ`, `ВРЕМЯ`. Строки — как на Album (та же подсветка играющего, hover, компактный режим); все текстовые ячейки с ellipsis.

**Треки в макете** (9): «Полынный свет» 4:12 FLAC; «Ток по проводам» 3:38 FLAC (Тихая Комната / Полынный свет); «Радиомолчание» 5:04 FLAC; «Письмо без адреса» 2:56 ALAC (Северный Шум / Запрещённые письма); «Ночной эфир» 6:21 FLAC (Мальва); «Красная линия» 3:11 FLAC (Отдельный Канал); «Молчание не входит» 4:45 MP3 320 (Кама); «Свободная частота» 7:02 FLAC (Тихая Комната); «Без подписи» 3:29 FLAC (Гость).

---

### 7. Shell — шапка, сайдбар, нижний плеер

**Шапка:** высота 46px, фон `#0E0E10`, `border-bottom: 1px solid #1E1E22`, `padding: 0 16px 0 18px`, `justify-content: space-between`.
- Слева (gap 22px): лого (квадрат 9×9 + `PRIVY/STREAM` Manrope 700 11px `.2em`); крошки `/{screen}` (+ ` / {код релиза}` на Album), Mono 400 10px `#4E4C48`; **чип текущего узла** — `padding: 6px 10px`, `border: 1px solid #2E2E33`, gap 8px: мигающая точка 6×6 `#D8452B` (анимация `pv-blink` 1.8s), host Mono 600 10px `#C8C6C0`, `{ping} ms ▾` Mono 400 10px `#4E4C48`; клик → Servers.
- Справа (gap 10px): переключатель DESKTOP/MOBILE и тумблер плотности `ВОЗДУШНО/ПЛОТНО` (см. «Что не переносить»), кнопка `ВЫХОД`. Кнопки: `padding: 7px 11px`, `border: 1px solid #26262A`, Mono 600 10px `.14em` `#8E8C86`, hover `border-color: #4A4A50`, `color: #E8E6E1`.

**Сайдбар:** ширина 216px, фон `#0E0E10`, `border-right: 1px solid #1E1E22`, `padding: 18px 0 16px`, `justify-content: space-between`.
- Заголовок `НАВИГАЦИЯ` (Mono 600 10px `.18em` `#4E4C48`, `padding: 0 18px 10px`).
- Пункты: `СЕРВЕРЫ` (счётчик `{online}/{всего}`), `КАТАЛОГ` (число релизов на узле), `АРТИСТ`, `РЕЛИЗ`. Пункт: `padding: 11px 18px` (компактно 8px), `justify-content: space-between`, `border-left: 2px solid transparent`, метка Mono 600 12px `.1em` `#8E8C86`, счётчик Mono 400 10px `#4E4C48`; активный — текст `#F2F0EB`, фон `#17171A`, `border-left-color: #D8452B`; hover — `#E8E6E1` на `#141417`. (В приложении набор пунктов: Серверы, Каталог, Библиотека, Настройки; `АРТИСТ`/`РЕЛИЗ` в макете — быстрый доступ к экранам.)
- Низ (`padding: 0 18px`, gap 12px, над разделителем `#1E1E22`): строки КАНАЛ `FLAC 24/96`, БУФЕР `96%`, УЗЕЛ `{name}` (Mono 400 10px, ключ `#6E6C67`, значение `#C8C6C0`); ниже кликабельная строка `СМЕНИТЬ УЗЕЛ · {n} ОНЛАЙН` с мигающей точкой → Servers.

**Нижний плеер:** высота 76px, фон `#101012`, `border-top: 1px solid #26262A`, `grid-template-columns: 264px 1fr 264px`, gap 18px, `padding: 0 18px`.
- Слева: миниатюра 44×44 (плейсхолдер `repeating-linear-gradient(135deg, #1D1D21 0 5px, #141417 5px 10px)`, `border: 1px solid #2E2E33`) + название Manrope 600 13px и `{артист} · {релиз}` Mono 400 10px `#6E6C67`, оба с ellipsis. Клик → полноэкранный плеер.
- Центр (gap 9px): кнопки по центру gap 16px — `◀◀` и `▶▶` Mono 500 12px `#8E8C86` (hover `#E8E6E1`), между ними play/pause 34×34, фон `#D8452B`, глиф `❙❙`/`▶` Manrope 700 12px `#0B0B0C`, hover `#F0563A`. Ниже полоса: текущее время (Mono 500 10px `#8E8C86`, ширина 36px), трек прогресса `height: 4px`, фон `#26262A`, заполнение `#D8452B` по проценту, клик = seek по позиции курсора; справа длительность.
- Справа: индикатор уровня — 14 полос шириной 3px, gap 2px, высота контейнера 22px, последние 4 полосы `#D8452B`, остальные `#4A4A50`, `transition: height .35s ease`; при паузе все 8%. Рядом Mono 600 9px/1.5 `#6E6C67` `.12em`: «24 BIT / 96 kHz / NO LOG».

---

### 8. Player (fullscreen) — полноэкранный плеер

**Purpose:** «сейчас играет» + очередь.

**Layout:** `position: fixed; inset: 0`, фон `#0A0A0B`, `z-index: 40`, появление — `pv-rise` .25s.
- Полоса сверху: высота 52px, `padding: 0 26px`, `border-bottom: 1px solid #1E1E22`, `justify-content: space-between`: слева `ВОСПРОИЗВЕДЕНИЕ · ПРИВАТНАЯ СЕССИЯ` (Mono 600 10px `.2em` `#D8452B`), справа `СВЕРНУТЬ ✕` (Mono 600 10px `.16em` `#8E8C86`).
- Тело: `grid-template-columns: 1.25fr .75fr`.
- **Левая часть** (`padding: 40px 56px`, `justify-content: center`, gap 32px):
  - Герой: `display: flex; flex-wrap: wrap; gap: 32px; align-items: flex-end`. Обложка 210×210 (плейсхолдер, `border: 1px solid #2E2E33`). Текстовый блок `flex: 1 1 340px; min-width: 0`, gap 12px: релиз Mono 600 11px `.16em` `#8E8C86`; название — Manrope 800, `font-size: clamp(30px, 3.6vw, 54px)`, `line-height: .95`, `letter-spacing: -.035em`, `overflow-wrap: anywhere` (важно: длинные слова не должны выдавливать раскладку); артист Mono 500 14px `#D8452B`.
  - Волна: 72 полосы, `flex: 1` каждая, высота контейнера 84px, `align-items: flex-end`, gap 2px, `border-bottom: 1px solid #1E1E22`. Пройденная часть `#D8452B`, остальное `#26262A`, `transition: background .2s`. Высоты в макете — детерминированная функция от индекса; в приложении — реальные пики (генерировать на узле при загрузке релиза и отдавать вместе с метаданными, либо считать через Web Audio при первом прослушивании и кэшировать).
  - Под волной: текущее время / `{формат} · 1411 kbps` (`#4E4C48`) / длительность, Mono 500 11px `#8E8C86`.
  - Транспорт (gap 20px): `◀◀` и `▶▶` — `padding: 12px 16px`, `border: 1px solid #2E2E33`, Mono 500 12px `#C8C6C0`; play/pause 62×62 фон `#D8452B`, глиф Manrope 700 20px `#0B0B0C`; через `margin-left: 12px` — `ПОВТОР` и `СЛУЧАЙНО` (`padding: 12px 14px`, обводка, Mono 600 10px `.12em` `#8E8C86`).
- **Правая часть** — очередь: `border-left: 1px solid #1E1E22`, фон `#0D0D0F`. Заголовок `ОЧЕРЕДЬ · {n}` (`padding: 18px 22px 12px`, Mono 600 10px `.18em` `#4E4C48`, `border-bottom`). Список со своим скроллом; строка: `padding: 11px 22px`, gap 12px, `border-bottom: 1px solid #16161A`, номер Mono 500 10px (ширина 20px), название Manrope 600 12px + артист Mono 400 10px `#5A5854` (оба ellipsis), длительность Mono 500 10px `#6E6C67`; играющая строка — фон `rgba(216,69,43,.08)`, номер `#D8452B`, название `#F2F0EB`. Футер (`padding: 14px 22px`, `border-top`): `ИСТОРИЯ` — `НЕ ПИШЕТСЯ` (`#D8452B`), `УЗЕЛ` — `{name} · {ping} ms`.

---

## Interactions & Behavior

**Навигация:** сайдбар и крошки; Catalog → Album (клик по карточке), Album → Artist (клик по имени), Artist → Album (клик по релизу в дискографии), `← КАТАЛОГ` / `← НАЗАД` — возврат в каталог. Чип узла в шапке, строка «СМЕНИТЬ УЗЕЛ» в сайдбаре и кнопка из пустого состояния ведут на Servers.

**Смена узла:** промежуточное состояние «ПОДКЛЮЧЕНИЕ…» (мигающая точка) → успех: активный узел меняется, приложение уходит на Catalog, поиск и фильтры сбрасываются, каталог и поиск скоупятся по новому узлу (заголовок, счётчик релизов, мобильная полка). Локальная фонотека и очередь воспроизведения не сбрасываются. Ошибка подключения — инлайн-сообщение на экране Servers, активный узел остаётся прежним.

**Плеер:** play/pause; next/prev — переход по очереди с зацикливанием, позиция сбрасывается на 0; клик по треку в любом списке — начать его и подсветить строку; seek — клик по полосе прогресса (доля от ширины). В макете прогресс идёт по таймеру 900 мс (демо) — заменить на `timeupdate` от `<audio>`. Индикатор уровня и волна анимированы; при паузе уровень падает до минимума.

**Анимации:** `pv-blink` — `0%,45% {opacity:1} 50%,100% {opacity:.15}`, применяется к статус-точкам (1.6–1.8s infinite; 0.7s при подключении). `pv-rise` — `from {translateY(8px); opacity:0} to {translateY(0); opacity:1}`, применяется к карточкам каталога (.3s ease) и полноэкранному плееру (.25s ease). Hover: кнопка-акцент → `#F0563A`; обводка → `border-color: #4A4A50`, `color: #E8E6E1`; строка списка → `background: rgba(255,255,255,.03)`; карточка → `opacity: .82`; текст-ссылка → `#E8E6E1`.

**Плотность:** тумблер `ВОЗДУШНО / ПЛОТНО` меняет вертикальные паддинги (контент `34px 44px 46px` → `20px 26px 30px`, строки списков `12px` → `8px`, строки узлов `17px` → `12px`, пункты сайдбара `11px` → `8px`) и размер карточек каталога (`minmax(194px)`/gap 26px → `minmax(150px)`/gap 16px). В приложении — настройка, а не кнопка в шапке.

**Состояния, которых в макете нет и которые нужны в приложении:** скелетоны загрузки каталога/треклиста, ошибка запроса к узлу (узел отвалился во время сессии → вернуть чип узла в предупреждающее состояние), прогресс загрузки трека в фонотеку, обновление токена и повторный логин при 401, оффлайн-режим (доступна только Library).

**Адаптивность окна:** окно ресайзится свободно. Строки узлов, шапки экранов, герой плеера и сетка карточек уже переносятся; проверить на 1280×720 (минимальная ширина окна — разумно 1100) и на 4K. Фиксированные значения — только сайдбар 216px и плеер 76px.

## State Management

Состояние макета (для переноса в Zustand/контексты):

- **Auth:** `authed`, `mode: 'login'|'register'`, `login`, `pass`, `birth`, `err`. В приложении — токены по узлам: `Map<host, {access, refresh}>`, в keychain через Rust-команды, не в localStorage.
- **Серверы:** `servers: Server[]` (id, name, host, owner, releases, ping, status, mode, note), `serverId` — активный узел, `newHost` — поле добавления, `srvErr`, `connecting` — id узла в процессе подключения. Список узлов персистится локально (файл настроек/SQLite).
- **Навигация:** `screen: 'servers'|'catalog'|'album'|'artist'|'library'`, `albumIdx`, `artistName` → в приложении роутер (`/servers`, `/catalog`, `/album/:id`, `/artist/:id`, `/library`).
- **Каталог:** `query`, `filter`. Кэш данных — по ключу `[host, resource]`, чтобы данные разных узлов не смешивались (TanStack Query подходит).
- **Плеер:** `playing`, `idx` (позиция в очереди), `pos` (процент), очередь. Персистить трек и позицию между запусками.
- **Вид:** `dense` (плотность) — в настройки; `view: 'desktop'|'mobile'` — выбросить.

**Данные, которые клиент запрашивает у узла:** дескриптор узла (имя, владелец, тип доступа, версия API), каталог релизов, треклист релиза, поиск, стрим трека (range-запросы), обложки, пики волны. Формат задаёт заказчик — макет подразумевает, что индексирует узел, а не клиент.

## Design Tokens

**Цвета**

| Назначение | Значение |
|---|---|
| Фон приложения | `#0B0B0C` |
| Фон панелей (шапка, сайдбар, левая колонка логина) | `#0E0E10` |
| Фон плеера | `#101012` |
| Фон карточки / приподнятой поверхности | `#121214` |
| Фон активного пункта | `#17171A` |
| Фон инпута | `#0C0C0E` |
| Фон очереди (fullscreen) | `#0D0D0F` |
| Фон fullscreen-плеера | `#0A0A0B` |
| Разделитель основной | `#1E1E22` |
| Разделитель строк списка | `#17171B` / `#16161A` / `#1B1B1F` |
| Рамка элементов управления | `#26262A` |
| Рамка инпута / кнопки-обводки | `#2E2E33` |
| Рамка hover | `#4A4A50` |
| Акцент | `#D8452B` |
| Акцент hover | `#F0563A` |
| Акцент, текст ошибки | `#F0765C` |
| Подложка акцента (активная строка) | `rgba(216,69,43,.07–.08)` |
| Подложка ошибки | `rgba(216,69,43,.1)` |
| Статус online | `#7FAE6B` |
| Статус подключения | `#E8C24B` |
| Текст основной | `#E8E6E1` |
| Текст яркий (играющий трек) | `#F2F0EB` |
| Текст вторичный | `#C8C6C0` |
| Текст третичный | `#8E8C86` |
| Текст приглушённый | `#6E6C67` |
| Текст служебный | `#5A5854` |
| Текст едва заметный | `#4E4C48` |
| Точка offline | `#3A3A40` |
| Полосы индикатора (неактивные) | `#4A4A50`, `#26262A` |
| Текст на акценте | `#0B0B0C` |

**Типографика**

- Дисплей/интерфейс: **Manrope** (400–800). Моно/метаданные: **IBM Plex Mono** (400/500/600). Подключение: Google Fonts, сабсеты `cyrillic,cyrillic-ext,latin` (кириллица обязательна — весь интерфейс на русском).
- Шкала: H1 логина 52px/.98 `-.03em` (700); имя артиста 68px/.92 `-.04em` (800); название трека в плеере `clamp(30px, 3.6vw, 54px)`/.95 `-.035em` (800); название релиза 46px/.98 `-.03em` (700); H2 экрана 34px/1 `-.02em` (700); статистика 22px (600); пустое состояние 16px/1.5 (500); имя узла 15px (600); абзац 15px/1.6 и 13px/1.6 (400); название трека/карточки 13–14px (600); мета-подписи Mono 9–12px.
- Трекинг у моно-меток: надзаголовки `.2em`, лейблы `.16em`, служебные `.1–.14em`. Все моно-метки — капсом.

**Отступы:** шкала 2 / 3 / 4 / 5 / 6 / 7 / 8 / 9 / 10 / 12 / 14 / 16 / 18 / 20 / 22 / 26 / 28 / 30 / 32 / 34 / 40 / 44 / 46 / 56 / 60 px. Контент-паддинг экрана `34px 44px 46px` (компактно `20px 26px 30px`). Gap списков 14px, сеток 16/26px.

**Радиусы:** нет. Все элементы — прямоугольные, это часть стиля («аппаратный» вид). Не добавлять `border-radius`.

**Тени:** нет. Иерархия строится на фоне и рамках 1px.

**Фиксированные размеры:** шапка 46px, сайдбар 216px, плеер 76px, полоса fullscreen 52px, миниатюра 44px, обложка fullscreen 210px, кнопка play 34px (в fullscreen 62px), статус-точка 7px, полоса прогресса 4px.

**Паттерны-плейсхолдеры:** обложки — `repeating-linear-gradient(135deg, #17171A 0 7px, #121215 7px 14px)`; миниатюры — `repeating-linear-gradient(135deg, #1D1D21 0 5px, #141417 5px 10px)`; фото артиста — `repeating-linear-gradient(115deg, #16161A 0 9px, #111114 9px 18px)`; фон мобильной витрины — `repeating-linear-gradient(45deg, #0C0C0E 0 12px, #0A0A0B 12px 24px)`. Скроллбар: трек `#101012`, ползунок `#2A2A2E`, ширина 8px.

## Assets

Изображений в макете нет — все обложки и фото артистов — CSS-плейсхолдеры с подписью и указанием размера (обложка 1000×1000, фото артиста 2400×720). Иконки не используются: транспорт плеера и стрелки набраны текстом (`▶`, `❙❙`, `◀◀`, `▶▶`, `←`, `→`, `✕`, `▾`, `+`). В приложении можно оставить текстовые глифы (это в характере стиля) либо взять минималистичный штриховой набор — но тогда единообразно и без заливок. Шрифты — Manrope и IBM Plex Mono (оба SIL OFL, можно вендорить локально, что для десктопного приложения предпочтительно — не зависеть от сети).

## Что не переносить в приложение

- **Переключатель DESKTOP / MOBILE** в шапке и весь мобильный вид (рамка 390×800 с таб-баром) — это витрина макета. Мобильный вид оставлен как референс на будущее (там же: мобильный экран узлов с добавлением по адресу, мини-плеер, таб-бар `ПОЛКА / УЗЛЫ / ФОНОТЕКА / Я`), но десктопное приложение его не содержит.
- **Тумблер плотности** в шапке — перенести в настройки.
- **Демо-таймер прогресса** (900 мс) — заменить на события `<audio>`.
- **Мок-данные** (`ALBUMS`, `TRACKS`, `SERVERS`, `SRV_OF`) — за слой API-клиента с тем же интерфейсом, чтобы UI не менялся при подключении реального узла.
- **Детерминированные высоты волны и уровня** — на реальные пики / анализ аудио.

## Files

- `Privy Stream.dc.html` — макет: все экраны, состояния и логика (шаблон + класс логики в одном файле).
- `support.js` — рантайм, нужен только для открытия макета в браузере; в приложение не переносится.

Открывать через локальный сервер: `npx serve .` в папке бандла.
